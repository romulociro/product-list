spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/productsdb?serverTimezone=UTC
    username: products_user
    password: prod123
  jpa:
    hibernate.ddl-auto: create
    show-sql: true

api:
  security:
    token:
      secret: my-secret-key